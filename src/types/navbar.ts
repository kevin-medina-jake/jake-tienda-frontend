export interface IDropDownMenu {
  id: string;
  name: string;
  slug: string;
  url_product: string;
}

export interface IBrandWithImage {
  id: string;
  name: string;
  slug: string;
  logo: string;
}
