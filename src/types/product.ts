export interface INewProducts {
  id: number;
  name: string;
  slug: string;
  image: string;
}

export interface IBestProduct {
  id: number;
  name: string;
  slug: string;
  image: string;
}

export interface IProductBond {
  id: number;
  title: string;
  description: string;
  slug: string;
  image: string;
}
